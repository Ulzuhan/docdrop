# DocDrop configuration. Every variable is optional; these are the defaults.
# Copy to .env.local for development, or use your service manager's environment file.

# PORT=3010
# DOCDROP_DATA_DIR=.docdrop-uploads

# Storage limits (bytes)
# DOCDROP_MAX_FILE_BYTES=10737418240    # 10 GB per file
# DOCDROP_MAX_TOTAL_BYTES=21474836480   # 20 GB total
# DOCDROP_CHUNK_BYTES=33554432          # 32 MiB per chunk

# Maximum duration of a single request. Node's default is 5 minutes, which cuts
# large uploads off mid-transfer; scripts/start.js raises it to 12h.
# DOCDROP_REQUEST_TIMEOUT_MS=43200000

# Dashboard password — OPTIONAL. Without these the service runs OPEN: anyone who
# reaches it can upload and see the full listing. Generate them with:
#   npm run set-password
# DOCDROP_PASSWORD_HASH=
# DOCDROP_SESSION_SECRET=
